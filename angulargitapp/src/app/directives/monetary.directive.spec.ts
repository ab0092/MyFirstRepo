import { MonetaryDirective } from './monetary.directive';

describe('MonetaryDirective', () => {
  it('should create an instance', () => {
    const directive = new MonetaryDirective();
    expect(directive).toBeTruthy();
  });
});
